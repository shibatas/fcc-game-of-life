//number of cells, [x, y]
const size = [50, 30];
//default speed, milliseconds frame rate
const rate = 250;

//density of live cells when generating random board
const density = 0.3;

export {size, rate, density}
